# Grailed Plus Preview Assets

Drop these files in this folder so the project preview renders media:

1. `preview.webm` (preferred)
2. `preview.mp4` (fallback)
3. `poster.webp`

The portfolio UI reads from:

- `/projects/grailed-plus/preview.webm`
- `/projects/grailed-plus/preview.mp4`
- `/projects/grailed-plus/poster.webp`
