rules_version = '2';


service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // allow write if admind
      allow write: if firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.isAdmin;
      // allow read for everyone
      allow read: if true;
    }
  }
}